node_modules
data/
api/_piston

# Package manifests are formatted; anything else under packages/ is build
# output - an extracted runtime tree left by `piston build-pkg`, or a built
# archive.
packages/*/*/
packages/*.pkg.tar.gz
packages/index
